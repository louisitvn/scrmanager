== README

Setup: 

* Migrate
* Seed

Description:

The application will trigger the lib/scraper.rb process which scrapes data and push into the database
Be sure to setup the DATABASE_URL correctly which is required by lib/scraper.rb
